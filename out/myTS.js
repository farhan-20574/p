"use strict";
const fileData = (input) => {
    if (input === 'string') {
        return input.toLocaleLowerCase();
    }
    else {
        let sum = 0;
        for (const element of input) {
        }
    }
};
